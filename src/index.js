import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';
import reportWebVitals from './reportWebVitals';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

// Переписать create listing
// Починить положение элементов на странице (карточки, футер)
// Добавить маршрутизацию по страницам (роутинг)
// Маршрутизировать кнопки на карточках
// Исправить стили
// Добавить фон на сайт
// Залить всё на гит

reportWebVitals();
